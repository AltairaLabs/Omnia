---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: arenadevsessions.omnia.altairalabs.ai
spec:
  group: omnia.altairalabs.ai
  names:
    kind: ArenaDevSession
    listKind: ArenaDevSessionList
    plural: arenadevsessions
    shortNames:
    - ads
    singular: arenadevsession
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.projectId
      name: Project
      type: string
    - jsonPath: .status.endpoint
      name: Endpoint
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ArenaDevSession is the Schema for the arenadevsessions API.
          It represents an ephemeral interactive testing session for an Arena project.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ArenaDevSessionSpec defines the desired state of ArenaDevSession.
              A dev session creates an ephemeral dev console for interactive agent testing.
            properties:
              idleTimeout:
                default: 30m
                description: |-
                  idleTimeout specifies how long the session can be idle before cleanup.
                  Default is 30 minutes.
                type: string
              image:
                description: image overrides the default dev console image.
                type: string
              projectId:
                description: projectID is the Arena project being tested.
                type: string
              resources:
                description: resources overrides the default resource requests/limits.
                properties:
                  limits:
                    additionalProperties:
                      type: string
                    description: limits describes maximum resources allowed.
                    type: object
                  requests:
                    additionalProperties:
                      type: string
                    description: requests describes minimum resources required.
                    type: object
                type: object
              workspace:
                description: workspace is the workspace name (for reference/labeling).
                type: string
            required:
            - projectId
            - workspace
            type: object
          status:
            description: ArenaDevSessionStatus defines the observed state of ArenaDevSession.
            properties:
              conditions:
                description: conditions represent the current state of the dev session.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              endpoint:
                description: |-
                  endpoint is the WebSocket URL to connect to the dev console.
                  Format: ws://arena-dev-console-{name}.{namespace}.svc:8080/ws
                type: string
              lastActivityAt:
                description: |-
                  lastActivityAt is the timestamp of the last client activity.
                  Used for idle timeout cleanup.
                format: date-time
                type: string
              message:
                description: message provides additional status information.
                type: string
              phase:
                description: phase represents the current lifecycle phase.
                enum:
                - Pending
                - Starting
                - Ready
                - Stopping
                - Stopped
                - Failed
                type: string
              serviceName:
                description: serviceName is the name of the created service.
                type: string
              startedAt:
                description: startedAt is when the dev console became ready.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
