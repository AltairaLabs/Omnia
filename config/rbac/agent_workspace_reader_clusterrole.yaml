# ClusterRole granting agent pods (facade/runtime) read access to Workspace CRDs.
# Agents need this to resolve per-workspace service URLs from
# Workspace.status.services. The AgentRuntimeReconciler creates a
# ClusterRoleBinding per agent that references this ClusterRole by the name
# passed to --agent-workspace-reader-clusterrole (default:
# "omnia-agent-workspace-reader").
#
# Without this file, kustomize-based deployments (including `make deploy` used
# by e2e) do not create the ClusterRole, leaving the per-agent CRBs dangling
# and breaking Workspace status resolution in agent pods. The Helm chart has
# its own equivalent template under
# charts/omnia/templates/agent-workspace-reader-clusterrole.yaml.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: omnia-agent-workspace-reader
rules:
  - apiGroups:
      - omnia.altairalabs.ai
    resources:
      - workspaces
    verbs:
      - get
      - list
      - watch
