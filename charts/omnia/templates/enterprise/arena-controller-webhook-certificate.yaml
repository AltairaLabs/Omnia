{{- if and .Values.enterprise.enabled .Values.webhook.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "omnia.fullname" . }}-arena-controller-webhook-cert
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "omnia.labels" . | nindent 4 }}
spec:
  secretName: {{ .Values.enterprise.arena.controller.webhook.certSecretName }}
  dnsNames:
    - {{ include "omnia.fullname" . }}-arena-controller-webhook.{{ .Release.Namespace }}.svc
    - {{ include "omnia.fullname" . }}-arena-controller-webhook.{{ .Release.Namespace }}.svc.cluster.local
  issuerRef:
    name: {{ include "omnia.fullname" . }}-webhook-selfsign
    kind: Issuer
{{- end }}
