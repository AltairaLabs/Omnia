{{- if .Values.doctor.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "omnia.doctor.fullname" . }}
  labels:
    {{- include "omnia.doctor.labels" . | nindent 4 }}
{{- end }}
