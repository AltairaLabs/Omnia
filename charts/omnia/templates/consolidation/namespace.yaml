{{- if .Values.consolidation.systemPacks.enabled }}
apiVersion: v1
kind: Namespace
metadata:
  name: omnia-system-packs
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service | quote }}
    app.kubernetes.io/component: memory-consolidation
    app.kubernetes.io/part-of: omnia
{{- end }}
