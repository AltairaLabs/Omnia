---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: sessionretentionpolicies.omnia.altairalabs.ai
spec:
  group: omnia.altairalabs.ai
  names:
    kind: SessionRetentionPolicy
    listKind: SessionRetentionPolicyList
    plural: sessionretentionpolicies
    singular: sessionretentionpolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.default.hotCache.ttlAfterInactive
      name: Hot Cache TTL
      type: string
    - jsonPath: .spec.default.warmStore.retentionDays
      name: Warm Days
      type: integer
    - jsonPath: .spec.default.coldArchive.enabled
      name: Cold Archive
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SessionRetentionPolicy is the Schema for the sessionretentionpolicies API.
          It defines retention rules for session history data across hot/warm/cold storage tiers.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of SessionRetentionPolicy
            properties:
              default:
                description: default defines the default retention configuration across
                  all storage tiers.
                properties:
                  coldArchive:
                    description: coldArchive configures the cold archive tier (e.g.,
                      S3, GCS).
                    properties:
                      compactionSchedule:
                        default: 0 2 * * *
                        description: compactionSchedule is a cron expression for when
                          to run compaction/archival.
                        type: string
                      enabled:
                        default: false
                        description: enabled specifies whether cold archival is active.
                        type: boolean
                      retentionDays:
                        description: |-
                          retentionDays is the number of days to retain data in the cold archive.
                          Required when enabled is true.
                        format: int32
                        maximum: 36500
                        minimum: 1
                        type: integer
                    type: object
                    x-kubernetes-validations:
                    - message: retentionDays is required when cold archive is enabled
                      rule: '!self.enabled || (has(self.retentionDays) && self.retentionDays
                        > 0)'
                  hotCache:
                    description: hotCache configures the Redis hot cache tier.
                    properties:
                      enabled:
                        default: true
                        description: enabled specifies whether the hot cache is active.
                        type: boolean
                      maxMessagesPerSession:
                        description: maxMessagesPerSession is the maximum number of
                          messages per session in the hot cache.
                        format: int32
                        minimum: 1
                        type: integer
                      maxSessions:
                        description: maxSessions is the maximum number of sessions
                          to keep in the hot cache.
                        format: int32
                        minimum: 1
                        type: integer
                      ttlAfterInactive:
                        default: 24h
                        description: |-
                          ttlAfterInactive is the duration after which inactive sessions are evicted from the hot cache.
                          Must be a Go duration string (e.g., "24h", "30m", "1h30m").
                        pattern: ^([0-9]+h)?([0-9]+m)?([0-9]+s)?$
                        type: string
                    type: object
                  warmStore:
                    description: warmStore configures the Postgres warm store tier.
                    properties:
                      partitionBy:
                        default: week
                        description: partitionBy defines the partitioning strategy
                          for warm store tables.
                        enum:
                        - week
                        type: string
                      retentionDays:
                        default: 7
                        description: retentionDays is the number of days to retain
                          data in the warm store.
                        format: int32
                        maximum: 3650
                        minimum: 1
                        type: integer
                    type: object
                type: object
              perWorkspace:
                additionalProperties:
                  description: |-
                    WorkspaceRetentionOverride defines per-workspace retention overrides.
                    HotCache is not overridable per workspace — it is a shared Redis instance.
                  properties:
                    coldArchive:
                      description: coldArchive overrides the cold archive configuration
                        for this workspace.
                      properties:
                        compactionSchedule:
                          default: 0 2 * * *
                          description: compactionSchedule is a cron expression for
                            when to run compaction/archival.
                          type: string
                        enabled:
                          default: false
                          description: enabled specifies whether cold archival is
                            active.
                          type: boolean
                        retentionDays:
                          description: |-
                            retentionDays is the number of days to retain data in the cold archive.
                            Required when enabled is true.
                          format: int32
                          maximum: 36500
                          minimum: 1
                          type: integer
                      type: object
                      x-kubernetes-validations:
                      - message: retentionDays is required when cold archive is enabled
                        rule: '!self.enabled || (has(self.retentionDays) && self.retentionDays
                          > 0)'
                    warmStore:
                      description: warmStore overrides the warm store configuration
                        for this workspace.
                      properties:
                        partitionBy:
                          default: week
                          description: partitionBy defines the partitioning strategy
                            for warm store tables.
                          enum:
                          - week
                          type: string
                        retentionDays:
                          default: 7
                          description: retentionDays is the number of days to retain
                            data in the warm store.
                          format: int32
                          maximum: 3650
                          minimum: 1
                          type: integer
                      type: object
                  type: object
                description: |-
                  perWorkspace defines per-workspace retention overrides.
                  Map keys are Workspace resource names.
                type: object
            required:
            - default
            type: object
          status:
            description: status defines the observed state of SessionRetentionPolicy
            properties:
              conditions:
                description: conditions represent the current state of the SessionRetentionPolicy
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: observedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: phase represents the current lifecycle phase of the policy.
                enum:
                - Active
                - Error
                type: string
              workspaceCount:
                description: workspaceCount is the number of workspaces with per-workspace
                  overrides that were resolved.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
